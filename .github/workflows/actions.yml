name: Cold Email Automation Run

on:
  schedule:
    # O'zbekiston vaqti bilan har 5 soatda (05:00, 10:00, 15:00, 20:00, 01:00)
    - cron: '0 0,5,10,15,20 * * *'
  workflow_dispatch: # Qo'lda ham "Run workflow" tugmasi bilan yurgazish uchun

# Bir vaqtda 2 ta jarayon ishlamasligi va yangisi kelganda oldingisi to'xtashi uchun:
concurrency:
  group: cold-email-automation
  cancel-in-progress: true

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install gspread google-auth

      - name: Run python script
        env:
          PYTHONUNBUFFERED: "1"
          GOOGLE_CREDENTIALS: ${{ secrets.GOOGLE_CREDENTIALS }}
          SHEET_NAME: ${{ secrets.SHEET_NAME }}
          ACCOUNTS_JSON: ${{ secrets.ACCOUNTS_JSON }}
        run: python -u run.py
